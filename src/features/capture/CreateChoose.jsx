import React from 'react';
import {Camera,Images,X} from 'lucide-react';
export default function CreateChoose({onPick,onCancel}){return <div className="screen"><div className="topbar"><div><div className="eyebrow">Capture</div><h1 className="title">Make something worth keeping</h1></div><button className="btn icon-btn" onClick={onCancel} aria-label="Close"><X size={18}/></button></div><div className="grid grid-2"><button className="card" style={{textAlign:'left'}} onClick={()=>onPick('dump')}><Images size={22} color="var(--amber)"/><h3 style={{marginTop:14}}>Start a dump</h3><p className="subtitle" style={{marginTop:6}}>A 3–20 item collection with a mood, context, and expiry.</p></button><button className="card" style={{textAlign:'left'}} onClick={()=>onPick('roll')}><Camera size={22} color="var(--cyan)"/><h3 style={{marginTop:14}}>Start a roll</h3><p className="subtitle" style={{marginTop:6}}>Choose a frame count, capture sequentially, then reveal.</p></button></div></div>}
