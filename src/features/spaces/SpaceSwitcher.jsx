import React from 'react';
import {ChevronLeft,Check,Plus} from 'lucide-react';
export default function SpaceSwitcher({spaces,activeSpaceId,setActiveSpaceId,onClose}){return <div className="screen"><div className="topbar"><div className="row"><button className="btn icon-btn" onClick={onClose}><ChevronLeft size={18}/></button><div><div className="eyebrow">Identity</div><h1 className="title" style={{fontSize:25}}>Switch space</h1></div></div></div><div className="stack">{spaces.map(s=><button className="card row" style={{textAlign:'left'}} key={s.id} onClick={()=>{setActiveSpaceId(s.id);onClose()}}><div className="avatar">{s.handle[0].toUpperCase()}</div><div style={{flex:1}}><strong>@{s.handle}</strong><p className="subtitle">{s.label} · {s.followers} followers</p></div>{s.id===activeSpaceId&&<Check size={18} color="var(--amber)"/>}</button>)}<button className="btn" style={{borderStyle:'dashed'}}><Plus size={15}/>New space</button></div></div>}
