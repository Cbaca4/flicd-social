import React from 'react';
import {Home,Compass,Camera,MessageCircle,User} from 'lucide-react';
const items=[['home',Home,'Home'],['discover',Compass,'Discover'],['messages',MessageCircle,'Messages'],['profile',User,'Profile']];
export default function BottomNav({screen,onNavigate,onCapture,unread=0,notificationsUnread=0}){return <nav className="bottom-dock" aria-label="Primary navigation">{items.slice(0,2).map(([key,Icon,label])=><button key={key} className={`nav-btn ${screen===key?'active':''}`} onClick={()=>onNavigate(key)} aria-label={label}><Icon size={20}/><span>{label}</span></button>)}<button className="nav-btn capture" onClick={onCapture} aria-label="Capture"><span className="capture-core"><Camera size={24}/></span></button>{items.slice(2).map(([key,Icon,label])=><button key={key} className={`nav-btn ${screen===key?'active':''}`} onClick={()=>onNavigate(key)} aria-label={label}><span style={{position:'relative'}}><Icon size={20}/>{key==='messages'&&unread>0&&<span style={{position:'absolute',top:-5,right:-7,width:13,height:13,borderRadius:99,background:'var(--amber)',color:'#17130a',fontSize:8,display:'grid',placeItems:'center',fontWeight:700}}>{unread>9?'9+':unread}</span>}{key==='profile'&&notificationsUnread>0&&<span style={{position:'absolute',top:-5,right:-7,width:13,height:13,borderRadius:99,background:'var(--cyan)',color:'#08221e',fontSize:8,display:'grid',placeItems:'center',fontWeight:700}}>{notificationsUnread>9?'9+':notificationsUnread}</span>}</span><span>{label}</span></button>)}</nav>}
